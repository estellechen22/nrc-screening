# 快速修复清单

## 🔴 立即修复 (CRITICAL)

### 问题1: 缺失数据库字段映射 

**受影响的文件：**
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/services/api_clients.py` (第15-70行)

**具体修复：**

在 `youtube_channel_stats()` 函数中的 `return` 语句（第60行）添加两个字段：

```python
# 当前代码（第60-70行）：
return {
    "channel_id": ch["id"],
    "name": snippet.get("title", ""),
    "handle": snippet.get("customUrl", channel_handle).lstrip("@"),
    "subs": int(stats.get("subscriberCount", 0)),
    "total_views": int(stats.get("viewCount", 0)),
    "video_count": int(stats.get("videoCount", 0)),
    "country": snippet.get("country", ""),
    "avatar_url": snippet.get("thumbnails", {}).get("default", {}).get("url", ""),
    "description": snippet.get("description", ""),
}

# 修改为：
return {
    "channel_id": ch["id"],
    "name": snippet.get("title", ""),
    "handle": snippet.get("customUrl", channel_handle).lstrip("@"),
    "subs": int(stats.get("subscriberCount", 0)),
    "total_views": int(stats.get("viewCount", 0)),
    "video_count": int(stats.get("videoCount", 0)),
    "country": snippet.get("country", ""),
    "content_lang": snippet.get("defaultLanguage", ""),  # 添加这行
    "category": "Gaming",  # 可从snippet中获取 categoryId 后映射，暂时设为默认值
    "avatar_url": snippet.get("thumbnails", {}).get("default", {}).get("url", ""),
    "description": snippet.get("description", ""),
}
```

同时修复Mock数据（第20-31行）：

```python
# 当前代码：
if not youtube_api_key:
    return {
        "channel_id": f"mock_{channel_handle}",
        "name": f"MockCreator_{channel_handle}",
        "handle": channel_handle.lstrip("@"),
        "subs": _stable_num(channel_handle, 50_000, 200_000),
        "total_views": _stable_num(channel_handle, 3_000_000, 20_000_000),
        "video_count": _stable_num(channel_handle, 80, 220),
        "country": "",
        "avatar_url": "",
        "description": "[占位数据] 等待接入 YouTube API Key",
    }

# 修改为：
if not youtube_api_key:
    return {
        "channel_id": f"mock_{channel_handle}",
        "name": f"MockCreator_{channel_handle}",
        "handle": channel_handle.lstrip("@"),
        "subs": _stable_num(channel_handle, 50_000, 200_000),
        "total_views": _stable_num(channel_handle, 3_000_000, 20_000_000),
        "video_count": _stable_num(channel_handle, 80, 220),
        "country": "",
        "content_lang": "en",  # 添加这行
        "category": "Gaming",  # 添加这行
        "avatar_url": "",
        "description": "[占位数据] 等待接入 YouTube API Key",
    }
```

**测试方法：**
```bash
# 启动后端服务
cd /Users/estelle/Desktop/Morefun/NRC/主播风险筛查
python -m uvicorn app:app --reload --port 8000

# 测试数据库是否能保存这些字段
# 提交一个筛查任务后检查SQLite数据库
sqlite3 data/screening.db "SELECT content_lang, category FROM creators LIMIT 1;"
```

---

## 🟠 本周修复 (WARNING)

### 问题2: 占位符关键词库

**受影响文件：**
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/app.py` (第44-70行)
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/data/keywords_political.json`

**具体修复：**

**方案A：清空占位符（推荐临时方案）**

编辑 `app.py` 第44-70行：

```python
# 当前：
placeholder_political = {
    "geo_sensitive": [
        "<待补充关键词1>",
        "<待补充关键词2>"
    ],
    "discrimination": [
        "<待补充关键词1>",
        "<待补充关键词2>"
    ],
    "brand_safety": [
        "<待补充关键词1>",
        "<待补充关键词2>"
    ]
}

# 改为：
placeholder_political = {
    "geo_sensitive": [],
    "discrimination": [],
    "brand_safety": []
}
```

同时编辑 `data/keywords_political.json`，将所有 `<待补充:...>` 替换为 `""` 或删除：

```json
{
  "geo_sensitive": [],
  "anti_china_narrative": [],
  "stigmatization": [],
  "hate_or_discrimination": []
}
```

**方案B：使用真实关键词**

如果已有真实词库，替换为实际值：
```python
placeholder_political = {
    "geo_sensitive": ["taiwan", "tibet", "xinjiang"],
    # ... 等等
}
```

---

### 问题3: Mock数据中的"mock"字段

**受影响文件：**
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/services/api_clients.py` (第154行)

**具体修复：**

在 `perspective_analyze()` 函数中，删除第154行的 `"mock": 1` 字段：

```python
# 当前代码（第141-155行）：
async def perspective_analyze(text: str) -> dict:
    keys = get_active_keys()
    perspective_api_key = keys["perspective_api_key"]

    if not perspective_api_key:
        return {
            "TOXICITY": 0.05,
            "SEVERE_TOXICITY": 0.01,
            "INSULT": 0.03,
            "IDENTITY_ATTACK": 0.02,
            "THREAT": 0.0,
            "PROFANITY": 0.04,
            "mock": 1,  # ← 删除这一行
        }

# 修改为：
async def perspective_analyze(text: str) -> dict:
    keys = get_active_keys()
    perspective_api_key = keys["perspective_api_key"]

    if not perspective_api_key:
        return {
            "TOXICITY": 0.05,
            "SEVERE_TOXICITY": 0.01,
            "INSULT": 0.03,
            "IDENTITY_ATTACK": 0.02,
            "THREAT": 0.0,
            "PROFANITY": 0.04,
        }
```

---

### 问题4: "unknown" channel_id 处理

**受影响文件：**
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/routers/screen.py` (第29行)
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/routers/batch.py` (第90行)

**具体修复方案A（简单）：**

在两个文件的顶部添加导入：
```python
import hashlib
```

然后修改具体行：

**screen.py:**
```python
# 当前（第29行）：
channel_id = extract_channel_id(req.url, platform) or "unknown"

# 修改为：
channel_id = extract_channel_id(req.url, platform) or hashlib.md5(req.url.encode()).hexdigest()[:12]
```

**batch.py:**
```python
# 当前（第90行）：
channel_id = extract_channel_id(url, platform) or "unknown"

# 修改为：
channel_id = extract_channel_id(url, platform) or hashlib.md5(url.encode()).hexdigest()[:12]
```

**修复方案B（更好）：**

在 `utils.py` 中增强 `extract_channel_id()` 函数返回值的稳定性，而不是使用"unknown"。

---

## 🟡 优化建议 (INFO)

### 问题5: 数据库查询索引

**受影响文件：**
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/database.py`

**具体修复：**

在 `SCHEMA` 常量的末尾添加两个索引（第64-67行后）：

```python
# 在现有索引后添加：
CREATE INDEX IF NOT EXISTS idx_screen_created ON screen_results(created_at DESC);
CREATE INDEX IF NOT EXISTS idx_creator_platform ON creators(platform);
```

完整示例：
```python
SCHEMA = """
CREATE TABLE IF NOT EXISTS creators (
    ...
);

... 其他表定义 ...

CREATE INDEX IF NOT EXISTS idx_screen_creator ON screen_results(creator_id);
CREATE INDEX IF NOT EXISTS idx_screen_status ON screen_results(status);
CREATE INDEX IF NOT EXISTS idx_layer_screen ON layer_results(screen_result_id);
CREATE INDEX IF NOT EXISTS idx_batch_status ON batch_jobs(status);
CREATE INDEX IF NOT EXISTS idx_screen_created ON screen_results(created_at DESC);  # 添加
CREATE INDEX IF NOT EXISTS idx_creator_platform ON creators(platform);  # 添加
"""
```

---

### 问题6: CORS配置

**受影响文件：**
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/app.py` (第27-33行)

**当前配置：**
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # ← 生产环境不安全
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**生产环境建议：**
```python
import os

ALLOWED_ORIGINS = os.getenv("ALLOWED_ORIGINS", "http://localhost:3000").split(",")

app.add_middleware(
    CORSMiddleware,
    allow_origins=ALLOWED_ORIGINS,
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["Content-Type", "X-Admin-Token"],
)
```

然后在 `.env` 中配置：
```
ALLOWED_ORIGINS=https://yourdomain.com,https://admin.yourdomain.com
```

---

### 问题7: Excel导出补充字段

**受影响文件：**
- `/Users/estelle/Desktop/Morefun/NRC/主播风险筛查/export/excel_exporter.py` (第41-50行)

**具体修复：**

在 `export_single_to_excel()` 函数中补充字段：

```python
# 当前代码（第41-50行）：
info_items = [
    ("平台", creator.get("platform", "")),
    ("URL", creator.get("url", "")),
    ("粉丝数", str(creator.get("subs", ""))),
    ("国家/地区", creator.get("country", "")),
]

# 修改为：
info_items = [
    ("平台", creator.get("platform", "")),
    ("URL", creator.get("url", "")),
    ("粉丝数", str(creator.get("subs", ""))),
    ("国家/地区", creator.get("country", "")),
    ("语言", creator.get("content_lang", "")),
    ("分类", creator.get("category", "")),
]
```

---

## ✅ 验证检查清单

修复完成后，按照以下清单检查：

- [ ] 数据库查询能否正确保存 `content_lang` 字段
- [ ] 数据库查询能否正确保存 `category` 字段
- [ ] 占位符关键词不再匹配虚假内容
- [ ] Mock API响应不包含"mock"字段
- [ ] 无法解析的channel_id不再为"unknown"
- [ ] 数据库查询性能有改善（检查query_time）
- [ ] Excel导出包含所有预期字段
- [ ] 没有Python导入错误

---

## 📌 修复优先级表

| 优先级 | 问题 | 影响 | 修复时间 |
|--------|------|------|--------|
| 1 | 缺失content_lang/category字段 | 🔴 严重 | 15分钟 |
| 2 | 清理占位符关键词 | 🟠 中等 | 10分钟 |
| 3 | 删除mock字段 | 🟡 轻微 | 5分钟 |
| 4 | 替换"unknown"字符串 | 🟡 轻微 | 10分钟 |
| 5 | 添加数据库索引 | 🟢 优化 | 5分钟 |
| 6 | CORS配置 | 🟢 优化 | 5分钟 |
| 7 | Excel导出补充字段 | 🟢 增强 | 10分钟 |

**总计修复时间：60分钟**

